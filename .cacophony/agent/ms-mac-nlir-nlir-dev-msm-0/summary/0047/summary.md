# Session summary — showcase: THE PLAIN RECAP (tone-knob over whole-thread SELECT)

## Goal
Harry's showcase directive; msm-0 lane. A distinct move + a real SELECT gap flagged.

## After state
- New move THE PLAIN RECAP `:~0^*-1` — the whole thread explained in plain, jargon-free language (newcomer recap); tone-knob sibling of EXEC BRIEF (@~0^*-1 formal). Dogfooded live on a 4-turn API-freeze debate; card + runnable proof (showcase-msm0-plain-recap.sh).
- Flagged a real SELECT gap: whole-role-channel ranges (~0^_*-1 / ~0^@*-1, "all of one role") error today (only indexed role picks ^_0/^_-1 work). A ^_* / ^@* range would unlock a true TWO-SIDES move. Noted as a deferred addressing/lexer feature (my lane), not an operator proposal.

## Diff summary
- Files: scripts/build-showcase.py (+plain-recap card), examples/CATALOG-msm0.md (+entry +gap note), examples/showcase-msm0-plain-recap.sh (new, +x), showcase/nlir-plain-recap.png (new).
- Tests: card dogfooded live; .sh proof runs.

## Operator-takeaway
Tone-knob over a SELECT: `@~0^*-1` (formal, exec) vs `:~0^*-1` (plain, newcomer) vs `~0^*-1` (terse, ping) — same whole-thread selection, register chosen by the leading op. Whole-role-channel SELECT is a real gap (deferred).
