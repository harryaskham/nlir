# Session summary — showcase: THE COMMON GROUND (role-channel merge)

## Goal
Harry's showcase directive; msm-0 lane. A distinct role-channel move completing the split/merge pair.

## After state
- New move THE COMMON GROUND `~(0^_-1 & 0^-1)` — merge both role channels (their side `^_` & ours `^`) and distil → the synthesis / where a debate lands. The flip-side of TWO-SIDES (split): same two channels, list vs join = contrast vs consensus. Honest: reports "still debating" if unconverged (doubles as an agreement check). Dogfooded live on the negotiation (clean resolution capture). Card + runnable proof.

## Diff summary
- Files: scripts/build-showcase.py (+common-ground card), examples/CATALOG-msm0.md (+entry + the split/merge pair note), examples/showcase-msm0-common-ground.sh (new,+x), showcase/nlir-common-ground.png (new).
- Tests: dogfood-run live.

## Operator-takeaway
Role-channel pair: `[~0^_-1, ~0^-1]` SPLIT (contrast) vs `~(0^_-1 & 0^-1)` MERGE (consensus). The SELECT structure (list vs join over the same two channels) chooses whether you get both sides or the synthesis.
