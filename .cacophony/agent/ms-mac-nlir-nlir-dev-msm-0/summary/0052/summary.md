# Session summary — verification pass + COMMON GROUND into phrasebook

## Goal
Harry's "really executing, not theory" bar. Audit all msm-0 showcase proofs; keep the phrasebook current.

## After state
- REAL-EXECUTION AUDIT: ran every examples/showcase-msm0-*.sh — 6/6 pass (common-ground, exec-brief, plain-recap, ticket, tone-knob, two-sides all produce live non-empty output from the binary). Every msm-0 card is a genuine execution, not hand-authored. (CATCH-UP/STANDUP/ARC additionally proven by the golf-msm0-*.sh set.)
- Added THE COMMON GROUND `~(0^_-1 & 0^-1)` to POWERMOVES.md (quick-ref row + SELECT-lane Moves list + card list), completing the msm-0 lane in the canonical phrasebook.

## Diff summary
- Files: examples/POWERMOVES.md (common-ground row + SELECT lane).
- Tests: 6/6 showcase-msm0 proof scripts execute live.

## Operator-takeaway
Provenance holds: 6/6 msm-0 showcase cards re-verified as live nlir executions. The msm-0 SELECT lane is complete in the phrasebook (8 moves: catch-up, exec-brief, standup, plain-recap, ticket, two-sides, common-ground, tone-knob).
