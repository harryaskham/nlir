# Session summary — showcase: EXEC BRIEF (multi-message digest, formal register)

## Goal
Harry's showcase directive; msm-0 lane = multi-message digest/select. Add a new rich move + reinforce real-execution provenance.

## After state
- New move EXEC BRIEF `@~0^*-1` — a messy multi-turn thread → one formal, forwardable paragraph (the "brief the VP in 10 min" move; formal-register companion to CATCH-UP). Dogfooded live on a real 5-turn incident thread (500s → 2026-expiry bug → rollback? → hotfix); output pasted verbatim.
- Added to examples/CATALOG-msm0.md + a "▶ reproduce any capture" block making every entry re-runnable (per Harry's "real executions, not theory").
- Card scripts/build-showcase.py + showcase/nlir-exec-brief.png, regenerated AFTER aur-1's ligature fix (8b8914c) so sigils render literally/typeably.

## Diff summary
- Files: examples/CATALOG-msm0.md, scripts/build-showcase.py (+exec-brief card), showcase/nlir-exec-brief.png (new).
- Tests: n/a (docs/showcase); move dogfooded live.

## Operator-takeaway
`@~0^*-1` = formalize∘summarize the whole thread → an exec-ready briefing. Register knob (`@`) over a whole-range digest. Every showcase capture is a live `--mode llm` run; catalog carries the reproduce command.
