# Session summary — golf #72 (what the question keeps) + target #70 (stopping-criterion)

## What landed
- examples/golf-aur1-72-projection.sh — WHAT THE QUESTION KEEPS: `~x? ≈ x?` (? absorbs a prior ~ —
  asking about a claim and asking about its gist give the SAME question), but `>x? ≠ x?` (expansion
  adds info the question keeps). This resolves the whole ?-family into ONE law: `?` is a projection
  onto INFORMATION content. It absorbs every prior op that does NOT add info — ! (#36), @ (#37), ~
  (#72) — and only > (#59 elaborator) changes x?, because expansion ADDS information the question
  keeps. Question the claim / its gist / its formal / its negation → same question; only fleshing
  it out first makes it bigger.
- examples/target-aur1-70-stopping.sh — 59th `?` framing: 'how do i know when to stop optimizing'?
  (40c) → "How do you know when to stop optimizing?" the SIGNAL you're done (vs #51 cessation, #47
  trigger).

## Notes
- Paren-echo fix still HOLDING for Harry's green-light (msm0 +1'd it at #71; surfacing in >(a&b)).
