# Session summary — golf #36 (question absorbs polarity) + target #34 (critique question)

## What landed
- examples/golf-aur1-36-absorb.sh — a LAW: `!x? ≈ x?`. Questioning a claim ABSORBS its negation —
  !x? returns identical to x?, because a yes/no question is polarity-NEUTRAL (it asks ABOUT the
  polarity, spanning both yes and no). In basis terms (msm0 #30): `!` moves the polarity axis, but
  `?` maps an assertion to a question about its polarity — invariant to where you started → ? ∘ ! = ?.
  Some operators don't just move axes, they COLLAPSE one. (Honest: expected a "skeptic's challenge",
  found an absorption law — the better result.)
- examples/target-aur1-34-critique.sh — 23rd `?` framing: 'whats wrong with this approach'? (34c) →
  "What is wrong with this approach?" A CRITIQUE question (vs #02 definition, #06 cause); ?
  normalises the contraction whats→What is.

## Notes
- Reproduced cleanly across two claims (x? == !x? both). msm0 also landed #36 (calculator report,
  deterministic LLM-off) — their lane, no collision.
