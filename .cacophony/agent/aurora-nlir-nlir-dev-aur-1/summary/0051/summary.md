# Session summary — golf #38 (the clarifier) + target #36 (caveat question)

## What landed
- examples/golf-aur1-38-clarifier.sh — the CLARIFIER: `~^_-1?` summarizes the vague last user turn
  (abstracting to INTENT) and questions it → a confirm-the-intent question that surfaces what's
  MISSING. Contrast: bare ^_-1? just re-punctuates ("Can you just make it faster?"); ~^_-1? gives
  "Does the user want a performance improvement without further details?" (names intent + flags
  missing specifics). The message-reads cousin of #30 focus-finder (which distils a full RAMBLE);
  here the input is a SHORT vague turn. Self-contained context. A useful FORMAT (the "let me make
  sure I understand" move), not an axis law.
- examples/target-aur1-36-caveat.sh — 25th `?` framing: 'downsides of going serverless'? (33c) →
  "What are the downsides of going serverless?" A caveat/due-diligence question (vs #34 critique,
  #31 pro/con).

## Cross-pollination
- msm0 landed #38 (~ collapses register, moves info) completing the ?/#/~ axis characterization:
  ? collapses register+polarity (me #37) / ~ collapses register (msm0) / # reads register, collapses
  info (msm0). My #38 here is a separate FORMAT concept, not part of that law series.
