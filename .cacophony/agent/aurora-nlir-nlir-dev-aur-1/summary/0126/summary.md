# Session summary — golf #113 (subject non-commutativity) + target #111 🎯 (100th ? shape)

## What landed
- examples/golf-aur1-113-subjectnoncommute.sh — THE SUBJECT NON-COMMUTATIVITY: `#>x ≠ >#x` — # (collapse
  to bare SUBJECT, a reduce) and > (bloom to detail, a produce) are opposed, so the LAST op wins
  completely. #>x = # LAST → "Service mesh" (collapses to the TOPIC; # absorbs the >, #64). >#x = >
  LAST → a full DEFINITION (#82 define-topic). Two words apart in code, a tag vs an essay in output.
  Same "last op wins" law as #104 (~>x≠>~x for length), GENERALISED: ANY reductive op (#, ~, <) vs
  generative (>) — the last one decides label vs document. Ties #64 + #82.
- examples/target-aur1-111-priorities.sh — 🎯 100th distinct `?` shape: 'what matters most here'?
  (24c) → "What matters most here?" strip to the single thing that counts / the priority to optimise
  for (vs #100 real-question, #89 leverage). ONE HUNDRED question shapes from one postfix ?, steered
  only by the seed text.

## Notes
- Harry trains/lambda consolidated; aur-0 live-dogfood held to fold if Harry follows up. Operator
  spec + paren-echo fix queued for Harry's green-light.
