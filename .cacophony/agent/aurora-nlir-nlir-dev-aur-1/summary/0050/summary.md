# Session summary — golf #37 (? factors through content) + target #35 (contingency question)

## What landed
- examples/golf-aur1-37-factor.sh — generalizes #36: `?` FACTORS THROUGH CONTENT. It STRIPS the
  pure-STYLE axes — @x? ≈ x? (register) and !x? ≈ x? (polarity, #36) — but RESPECTS information:
  >x? ≠ x? (expand ADDS reasons → a long multi-clause question). So ? is invariant to style,
  sensitive to substance. Mirrors msm0's ~ (factors through content). Draws the exact absorption
  boundary for ?.
- examples/target-aur1-35-contingency.sh — 24th `?` framing: 'what to do if the build fails'? (33c)
  → "What should I/you do if the build fails?" (pronoun floats). A RESPONSE-PLAN question (vs #06
  cause, #29 consequences).

## Cross-pollination
- Landed alongside msm0's #37 (# collapses the INFORMATION axis: #x≈#~x≈#>x). Near-DUALS: ? strips
  register+polarity / respects info; # respects register (fragile #28) / strips info. Together they
  map the axis-collapse taxonomy (? kills polarity+register, # kills information).
