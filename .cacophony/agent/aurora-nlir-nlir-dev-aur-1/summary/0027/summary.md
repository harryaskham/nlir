# Session summary — golf #14 (follow-up) + target #12 (where-question)

## What landed
- examples/golf-aur1-14-followup.sh — `^-1?`: question the assistant's last answer =
  the natural next question. "it does a sequential scan instead of using the index" →
  "Why does it do a sequential scan instead of using the index on customer_id?" An
  auto-Socratic "what to ask next" button. Distinct from #01 Socratic (~^-1? summarises
  first); here ? questions the RAW answer, drilling into specifics.
- examples/target-aur1-12-where.sh — 7th `?` mood: 'where are rust crates published'?
  (32c) → "Where are Rust crates published?" (exact). The "where are" seed shape steers
  ? to the locative frame.

## Notes
- Two concepts tried + rejected this tick: headline+lede [<~x,~x] (tiers near-identical),
  and abstraction ladder [#x,##x] (# is idempotent — subject of a noun phrase is a fixpoint).
- ? palette now SEVEN moods: how/what/why/should/how-much/when/where — a near-complete
  question generator from seed shape.
