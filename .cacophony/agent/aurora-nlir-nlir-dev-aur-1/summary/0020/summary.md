# Session summary — golf #07 (consensus) + target #05 (comparison question)

## What landed
- examples/golf-aur1-07-consensus.sh — `~[o1,o2,o3]`: summarise a list of competing
  opinions = the emergent CONSENSUS. 3 Rust-vs-Go stances → "landing on a compromise
  to rewrite only the hot path in Rust" (the position nobody stated but all circled).
  A vote-counter for language, not numbers.
- examples/target-aur1-05-compare.sh — `?` lane, fresh shape: 'difference between a
  mutex and a semaphore'? (45c) → "What is the difference between a mutex and a
  semaphore?" (exact). ? picks "what is" for a compare vs "how do I" for a fix.

## Operator-takeaway
`~[list]` turns N opinions into one settled line — consensus extraction. And ? reconstructs
different question CLASSES (compare vs how-to) from the seed's shape alone. Mechanism lanes
holding: ?/recursion/stack/consensus (me), @/ranges (msm0), >/: (aur2).
