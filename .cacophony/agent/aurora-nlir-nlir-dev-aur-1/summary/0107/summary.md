# Session summary — golf #94 (the skim-or-study) + target #92 (speed)

## What landed
- examples/golf-aur1-94-skimstudy.sh — THE SKIM-OR-STUDY: `[~x, >~x]` serves two readers from one
  source — ~x the SKIM (one line for the gist) + >~x the STUDY (a focused deep-dive). Both derive
  from the same essence: >~x distils to the core FIRST then expands THAT, so the deep-dive stays
  on-point (organised around the actual causes) instead of sprawling. Distinct from #44 BLUF ([~x,
  >x]): there the second half is >x (expands the WHOLE input, everything in order); here it's >~x
  (expands the DISTILLED core, the point in depth). Skim the line or study the focused body — the
  body never wanders.
- examples/target-aur1-92-speed.sh — 81st `?` framing: 'whats the fastest way to do this'? (35c) →
  "What's the fastest way to do this?" optimise for SPEED (vs #21 best-way, #85 simpler-way).

## Notes
- HARRY OPERATOR-PROPOSAL REQUEST (cacophony): posted 4 new-operator proposals grounded in real gaps
  — MAP `op*` (fmap over lists/ranges, fixes #77/#89/#91 list-inconsistency), THEREFORE `⊢x`
  (entailment/reason-forward, the gap the-consult died on), CONTRAST `a%b` (relational binary,
  de-overloads ~(a&b)), LENS `x.part` (focus within content, speculative). Team FYI posted to nlir.
  AWAIT Harry's pick; offered to spec + golf-prove any greenlit ones.
- Paren-echo fix still HOLDING for Harry's green-light (msm0 +1'd).
