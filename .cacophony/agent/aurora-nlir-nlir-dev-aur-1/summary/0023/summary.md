# Session summary — golf #10 (topic drift) + target #08 (decision question)

## What landed
- examples/golf-aur1-10-drift.sh — `[#^_0,#^_-1]`: subject of FIRST vs LAST user turn
  = topic drift. A thread opening on "CI pipeline for a Rust project" and wandering to
  "shrinking release binary size" reports that migration — before/after of attention.
  Temporal via POINT reads (^_0, ^_-1), the cousin of msm0's M^N ranges.
- examples/target-aur1-08-decision.sh — 4th `?` class: 'use REST or GraphQL for my API'?
  (33c) → "Should I use REST or GraphQL for my API?" (exact). ? now spans
  how-do-I/what-is/why/should-I, each inferred from seed shape.

## Operator-takeaway
Point reads (^_0 vs ^_-1) give a before/after of a conversation's focus — temporal without
ranges. And ? covers a 4th mood (decision) — one operator, four interrogative frames.
