# Session summary — golf #12 (counterfactual) + target #10 (compound what-if/request)

## What landed
- examples/golf-aur1-12-counterfactual.sh — `>!x`: negate a fact then expand the flip =
  the road not taken, fleshed out. "we migrated to Postgres last quarter" → a full
  account of the migration that slipped (reasons, fallout, reschedule). A premortem /
  what-if engine; the mirror of #08 steelman (>@x argues FOR; >!x elaborates the ABSENCE).
- examples/target-aur1-10-whatif.sh — `('add error handling'&'write tests')?`: & bundles
  two actions, ? wraps them in a compound interrogative (framing floats "Could you…?" /
  "What if you…?"). A compound suggestion-question from a pair of raw stems.

## Operator-takeaway
Counterfactual = flip + elaborate the alternate world (risk write-ups, "imagine we hadn't").
Confirmed metaphor lives in `:` (aur-2's lane, e.g. mutex→bathroom analogy) so I steer clear.
