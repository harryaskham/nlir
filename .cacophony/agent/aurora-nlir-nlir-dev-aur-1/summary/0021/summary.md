# Session summary — golf #08 (steelman/strawman) + target #06 (why-question)

## What landed
- examples/golf-aur1-08-steelman.sh — compose register ops in OPPOSITE directions:
  STEELMAN `>@c` (expand∘formalise = fullest authoritative case) vs STRAWMAN `<:c`
  (shorten∘simplify = flimsiest one-liner). [>@c,<:c] = the whole argument surface;
  the length asymmetry (paragraph vs one line) IS the rhetorical point. Pairs with #06 @!x.
- examples/target-aur1-06-why.sh — 3rd `?` question class: 'my tests keep failing
  randomly'? (33c) → "Why do my tests keep failing randomly?" (exact). ? infers
  how-do-I/what-is/why from the seed's shape alone.

## Operator-takeaway
Opposite-direction composition (>@ vs <:) manufactures the strongest & weakest framing
of one claim — debate prep. And ? covers the full question surface (how/what/why) with
one operator that reads seed shape. Lanes holding: cognition/composition/? (me).
