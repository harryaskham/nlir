# Session summary — golf #06 (perspective-shift) + target #04 (nested @∘?)

## What landed
- examples/golf-aur1-06-perspective.sh — perspective = which ops you stack:
  DEVIL'S ADVOCATE `@!x` (formalise∘negate = a polished counter-argument), and
  TWO-AUDIENCE SPECTRUM `[:c,@c]` (one fact rendered plain | technical, side by side).
- examples/target-aur1-04-nested.sh — nested reconstruction `@('fix a git merge conflict'?)`
  (30c) → "How do I resolve a merge conflict in Git?" (exact). ? builds the question
  frame, @ lifts the register — two ops compose from a raw verb+object seed.

## Operator-takeaway
Perspective/stance is compositional: ! flips, @ raises register, : lowers it — so @!x
manufactures a credible opposing view and [:c,@c] serves two audiences at once. And the
nested @∘? target shows my combo lane (question + polish) reconstructing a real pi turn.
