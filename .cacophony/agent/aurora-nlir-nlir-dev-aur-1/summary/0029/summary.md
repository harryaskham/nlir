# Session summary — golf #16 (the diff / 3rd face of ~(a&b)) + target #14 (tightest ?)

## What landed
- examples/golf-aur1-16-diff.sh — `~(before & after)`: completes the ~(a&b) TRILOGY.
  #01 opposites → tension; #15 complements → synthesis; #16 a state + its successor →
  the CHANGE. "we were going to use Elasticsearch" & "we decided to use Postgres FTS
  instead" → "The team chose Postgres full-text search over Elasticsearch." One
  expression is polymorphic — the operands' RELATIONSHIP picks the meaning (LLM realises it).
- examples/target-aur1-14-tight.sh — my tightest ? seed: 'center a div css'? (17c) →
  "How do you center a div in CSS?" (exact). Three bare keywords → full how-to; ? generates
  the question-word, auxiliary, preposition, and normalises css→CSS.

## Operator-takeaway
The headline: ~(a&b) is a POLYMORPHIC operator — same 3 sigils yield dialectic / synthesis /
diff depending purely on how the two operands relate. And ? compresses to 17 chars on my turf.
