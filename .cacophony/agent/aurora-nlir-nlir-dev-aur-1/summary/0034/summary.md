# Session summary — golf #21 (review kit) + target #19 (how-long question)

## What landed
- examples/golf-aur1-21-reviewkit.sh — `^-1;[!$,$?]`: push the assistant's last answer once,
  view it through two opposed lenses — !$ (the objection/red-team) and $? (the follow-up) —
  both reading the pushed value via $. Stitches together stack ($ peek) + #20 red-team + #14
  follow-up: a challenge-and-clarify card from one message read.
- examples/target-aur1-19-howlong.sh — 10th `?` shape: 'how long does a database migration
  take'? (37c) → "How long does a database migration take?" (exact). "how long" = duration,
  distinct from #09 "how much" = quantity — ? distinguishes from seed wording.

## Notes
- Rejected this tick for the target: <@'ramble' (crisp-status) — verbose seed→crisp output goes
  the WRONG way for target-golf (seed longer than target); and @(X&Y) request-with-reason left
  stray wrapping parens (grouped operands get parenthesised). Kept the game's terse→full spirit.
