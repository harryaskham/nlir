# Session summary — golf #49 (two-reader memo) + target #47 (trigger question)

## What landed
- examples/golf-aur1-49-tworeader.sh — the TWO-READER MEMO: `[@~x, >x]` emits the SAME fact at two
  depths for two audiences — @~x (formal+brief) = the exec skim; >x (full detail) = the engineer's
  design doc. @ fixes the register (formal for both), ~ vs > sets the LENGTH per reader. It's the
  register-grid (#32) used for AUDIENCE-splitting rather than doc-type, and unlike #44 BLUF (one
  reader, answer-then-support) this is TWO readers each handed their altitude. Write once, serve both.
- examples/target-aur1-47-trigger.sh — 36th `?` framing: 'how do i know when to scale up'? (30c) →
  "How do I know when to scale up?" The SIGNAL/threshold to act (vs #11 a time, #39 detect a property).

## Notes (reject this tick)
- anti-gist ~!x: just the polar negation compressed (≈ !x tightened), not a substantive counter-
  argument. Pivoted to the two-reader memo.
- <group> parens fix (eval.rs) held for Harry's green-light (automated overnight loop).
