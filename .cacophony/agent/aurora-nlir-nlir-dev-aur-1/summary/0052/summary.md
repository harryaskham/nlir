# Session summary — golf #39 (? is a projection) + target #37 (selection-method question)

## What landed
- examples/golf-aur1-39-projection.sh — the CAPSTONE ?-law: `?` is IDEMPOTENT (x? ≈ x?? ≈ x??? —
  questioning a question is a no-op). Combined with #36 (?! = ?, absorbs polarity) and #37 (?@ = ?,
  absorbs register), this is the exact signature of a PROJECTION operator: P² = P onto the
  question-subspace, flattening the style axes it ignores. Restyle / negate the input, or re-question
  the output — once projected you stay put. The cleanest object in the algebra: an operator that is
  its own square. Reproduced across two claims.
- examples/target-aur1-37-selection.sh — 26th `?` framing: 'how to choose between rest and graphql'?
  (38c) → "How do I/you choose between REST and GraphQL?" (pronoun floats). A DECISION-CRITERIA
  question (vs #32 which-wins, #20 which).

## Cross-pollination
- msm0 landed #39 (collapse BOUNDARY — # stops collapsing info on problem+solution conversations
  because ~ re-weights emphasis). Their lane (# / ranges); complements my ?-projection nicely
  (both probe where operator laws hold/break).
