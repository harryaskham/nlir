# Session summary — golf #15 (merge/synthesis) + target #13 (who-question)

## What landed
- examples/golf-aur1-15-merge.sh — `~(a & b)`: the SAME machine as #01's dialectic,
  but pointed at COMPLEMENTARY ideas → it fuses two requirements into one design line.
  "a REST API for our product catalog" + "clients need real-time updates" → "A REST API
  for the product catalog needs to push real-time price update notifications to clients."
  The constructive twin of the dialectic (opposites surface tension; complements fuse).
- examples/target-aur1-13-who.sh — 8th `?` mood: 'who maintains the rust standard library'?
  (37c) → "Who maintains the Rust standard library?" (exact).

## Operator-takeaway
One expression `~(a&b)` does dialectic OR synthesis depending on whether the operands
oppose or complement — a nice duality. And ? now spans the full core interrogative set
(who/what/when/where/why/how/how-much/should) — a complete question generator from seed shape.
